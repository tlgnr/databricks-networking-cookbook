---
#-----------------------------------
# VPC Endpoint Service
#-----------------------------------
ept-hub-nlb-${environment}-${region}:
  acceptance_required: true
  allowed_principals: 
    - arn:aws:iam::565502421330:role/private-connectivity-role-${region}
  name: ept-hub-nlb-${environment}-${region}
  network_load_balancer_names:
    - nlb-hub-${environment}-${region}
  vpc_name: vpc-hub-${environment}-${region}
